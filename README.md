# Wallpage: a browser 'screensaver' gallery of evolving generative scenes for TVs, second monitors and wall displays

Live: https://wallpage.sociobot.in — built by the Param Factory (`static-web`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
